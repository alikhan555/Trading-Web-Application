@using RapidWeb.Models

@{
    ViewBag.Title = "Department List";
    Layout = "~/Views/Shared/_Layout.cshtml";

    int roleId = ((User)HttpContext.Current.Session["User"]).RoleId;
}


<div class="col-xl-12 order-xl-1">
    <div class="card bg-secondary shadow">
        <div class="card-header bg-white border-0">
            <div class="row align-items-center">
                <div class="col-8">
                    <h3 class="mb-0">@ViewBag.Title</h3>
                </div>
                <div class="col-4">
                    <a class="float-right btn btn-primary btn-sm" href="@Url.Action("Create")">New Department</a>
                </div>
            </div>
        </div>

        <div class="card-body">
            <table id="Table" class="table text-align-center table-bordered table-grid">
                <thead class="text-primary font-weight-bold">
                    <tr>
                        <th scope="col">Dept ID</th>
                        <th scope="col">Dept Name</th>
                        <th scope="col">Options</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (Department department in Model)
                    {
                        <tr class="text-dark">
                            <td style="">
                                @department.PrefixId
                            </td>
                            <td style="">
                                @department.Name
                            </td>
                            <td>
                                <a href="@Url.Action("Edit", "Department", new {id = department.Id})" style="cursor: pointer" class="ml-2 text-primary EditUser"><i class="fa fa-pen"></i></a>

                                @if (roleId != Role.RTPUser)
                                {
                                    <a href="@Url.Action("ChangeActiveStatus", "Department", new {id = department.Id})" style="cursor: pointer" class="ml-2 text-primary ChangeActiveStatusUser">
                                        <i class='fa @(department.IsActive ? "fa-check" : "fa-times")'></i>
                                    </a>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts
{
    <script type="text/javascript">
        
        $(document).ready(function() {
            $("#Table").dataTable();
        });

        $(".form-inline").css({
            width: 1000
        });
    </script>
}
<style>
    .form-inline {
        display: block;
    }
</style>
