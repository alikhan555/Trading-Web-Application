
@{
    ViewBag.Title = "GetDynamicTable";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-xl-12 order-xl-1">

    <div id="SectionChartDiv" class="card bg-secondary shadow">
        <div class="card-header bg-white border-0">
            <div class="row align-items-center">
                <div class="col-8">
                    <h3 class="mb-0">DynamicTable</h3>
                </div>
                <div class="col-4 text-right">
                    <button id="BackToSectionFormBtn" type="button" class="btn btn-sm btn-primary">Back</button>
                </div>
            </div>
        </div>

        <div class="card-body">
            <div class="table-responsive">
                <table id="DynamicTable" class="table align-items-center table-flush">
                    <thead class="thead-light">
                        <tr>
                            <th scope="col">Product</th>
                            <th scope="col">Quantity</th>
                            <th scope="col">Cost</th>
                            <th scope="col">Remove</th>
                        </tr>
                    </thead>
                    <tbody>
                        @*<tr>
                            <td class="SequenceNo">
                                <select class="form-control form-control-alternative" data-val="true" data-val-number="The field RoleId must be a number." data-val-required="The RoleId field is required." id="User_RoleId" name="User.RoleId"></select>
                            </td>
                            <td class="CourseId">
                                <input type="text" id="Section-Name" class="form-control form-control-alternative">
                            </td>
                            <td class="CourseTitle">
                                <input type="text" id="Section-Name" class="form-control form-control-alternative">
                            </td>
                            <td align="center">
                                <button class="DeleteRow btn btn-danger btn-sm" type="button">
                                    <span aria-hidden="true">
                                        X
                                    </span>
                                </button>
                            </td>
                        </tr>*@
                    </tbody>
                    <tfoot>
                    <tr>
                        <td colspan="4">
                            <button id="AddNewRow" class="  btn btn-primary btn-sm btn-block" style="" type="button">
                                Add
                            </button>
                        </td>
                    </tr>
                    </tfoot>
                </table>

            </div>
        </div>
    </div>
</div>

@section Scripts
{
    @Scripts.Render("~/bundles/jquery");
    
    <script type="text/javascript">
        $(document).ready(function() {
            var productsList = [];
            LoadProducts();

            function LoadProducts() {
                $.ajax({
                    url: "/Product/GetProductAllProducts",
                    type: "GET",
                    success: function(data) {
                        productsList = data;
                    },
                    error: function() {
                    }
                });
            }

            $("#AddNewRow").click(function () {
                var options = "<option value>Select Product</option>";

                $.each(productsList,
                    function(index, item) {
                        options += "<option value=" + item.Id + ">" + item.Description + "</option>";
                    });

                var tbody = $("#DynamicTable tbody");
                var tr = "<tr>" +
                    "<td class=''>" +
                    "<select class='form-control form-control-alternative' data-val='true' data-val-number='The field RoleId must be a number.' data-val-required='The RoleId field is required.' id='User_RoleId' name='User.RoleId'>" +
                    options
                    +"</select>" +
                    "</td>" +
                    "<td class=''>" +
                    "<input type='text' class='form-control form-control-alternative'>" +
                    "</td>" +
                    "<td class=''>" +
                    "<input type='text' class='form-control form-control-alternative'>" +
                    "</td>" +
                    "<td align='center'>" +
                    "<button class='DeleteRow btn btn-danger btn-sm' type='button'>" +
                    "<span aria-hidden='true'>" +
                    "X" +
                    "</span>" +
                    "</button>" +
                    "</td>" +
                    "</tr>";
                tbody.append(tr);
            });

            $("#DynamicTable").on("click",".DeleteRow", function() {
                $(this).parent().parent().remove();
            });
        });
    </script>
}